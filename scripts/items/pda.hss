# Personal Digital Assistant
# By SlimeMaid
#
# Save anywhere! I wanna make this capable of saving,
# loading, etc...


plotscript, use pda, begin
    if ( check tag (tag:toggle) ), then, begin
        set tag (tag:toggle, false)
        save menu()
    end
end
